# 📱 Как проверить мобильную навигацию

## Текущая проблема: ДВЕ навигации

Сейчас у вас показываются:
1. **Header (вверху)** - с языком, темой, корзиной
2. **MobileNav (внизу)** - с Home, Menu, Cart, Profile

На мобильных это дублирование!

## Как проверить прямо сейчас:

### 1. В браузере
```
Откройте: http://localhost:3000
```

### 2. Включите мобильный вид (Chrome DevTools)
```
1. F12 или Cmd+Option+I (Mac)
2. Cmd+Shift+M (Toggle Device Toolbar)
3. Выберите iPhone 14 Pro или Galaxy S20
```

### 3. Что увидите:
- ❌ Две навигации (Header + MobileNav)
- ❌ Дублирование кнопок
- ❌ Теряется место на экране

## Решение (я уже применил):

### Header - скрыт на мобильных (< 768px):
```tsx
<header className="hidden md:block sticky top-0 z-50 ...">
  {/* Header показывается только на планшетах и desktop */}
</header>
```

### MobileNav - показан на мобильных (< 768px):
```tsx
<nav className="fixed bottom-0 md:hidden ...">
  {/* MobileNav только на телефонах */}
</nav>
```

## Как это работает:

### 📱 Мобильные (< 768px):
```
┌─────────────────────┐
│                     │ ← Нет Header
│    Контент          │
│                     │
├─────────────────────┤
│ 🏠 📋 🛒 👤        │ ← MobileNav внизу
└─────────────────────┘
```

### 💻 Desktop (≥ 768px):
```
┌─────────────────────┐
│ Logo  Lang Theme 🛒 │ ← Header вверху
├─────────────────────┤
│                     │
│    Контент          │
│                     │
└─────────────────────┘
(Нет MobileNav)
```

## Проверка в реальном времени:

1. **Откройте DevTools** (F12)
2. **Toggle Device Toolbar** (Cmd+Shift+M)
3. **Выберите устройство:**
   - iPhone SE (375px) - маленький
   - iPhone 14 Pro (390px) - средний
   - iPad (768px) - граница

4. **Смотрите что меняется:**
   - < 768px → только MobileNav внизу
   - ≥ 768px → только Header вверху

## Что уже настроено:

✅ Header скрывается на мобильных
✅ MobileNav скрывается на desktop
✅ Корзина есть в обеих навигациях
✅ Safe area для iPhone X+ (notch)
✅ Touch targets ≥ 44px

## Сейчас нужно:

1. Перезапустить dev сервер (изменения в header.tsx)
2. Проверить в браузере
3. Если всё ОК → commit и push

---

**Приложение работает на:**
- Local: http://localhost:3000
- Network: http://192.168.0.206:3000

Откройте любой из этих адресов и включите мобильный режим в DevTools!
