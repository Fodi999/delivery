# 📦 Checkout Components Structure

Модульная архитектура для лучшей организации и масштабируемости.

## 📁 Структура директорий

```
components/checkout/
├── core/                      # Основные UI компоненты
│   ├── PersonSelector.tsx     # Селектор количества персон
│   └── order-summary.tsx      # Сводка заказа
│
├── ai/                        # AI-функционал
│   ├── AIRecommendations.tsx  # AI рекомендации и предложения
│   └── ai-assistant.tsx       # AI ассистент
│
├── delivery/                  # Доставка и карты
│   └── DeliveryMapSection.tsx # Карта доставки с деталями
│
├── checkout-form.tsx          # Главная форма (оркестратор)
└── README.md                  # Эта документация
```

## 🎯 Принципы организации

### **Core** (`core/`)
Базовые UI компоненты, необходимые для оформления заказа:
- ✅ Без внешних зависимостей от AI
- ✅ Чистая логика UI
- ✅ Легко тестировать
- ✅ Переиспользуемые

**Примеры:**
- PersonSelector - выбор количества персон (1-4 + расширенный счётчик)
- order-summary - итоговая сводка заказа

### **AI** (`ai/`)
Все компоненты, связанные с AI-функционалом:
- 🤖 AI рекомендации по количеству еды
- 🎯 Умные предложения блюд
- 💬 AI ассистент для помощи клиентам
- ⚡ Асинхронная генерация контента

**Примеры:**
- AIRecommendations - карточки с AI рекомендациями + интерактивные кнопки
- ai-assistant - чат-помощник (будущее расширение)

### **Delivery** (`delivery/`)
Всё, что связано с доставкой:
- 🗺️ Интеграция карт (Mapbox)
- 📍 Геолокация и геокодирование
- 🚚 Расчёт расстояния и стоимости
- ⏱️ Время доставки

**Примеры:**
- DeliveryMapSection - карта + детали доставки (расстояние, время, цена)

## 📝 Использование

### Импорты в checkout-form.tsx:

```tsx
// Core components
import { PersonSelector } from "./core/PersonSelector";

// AI components
import { AIRecommendationCard, AISuggestions } from "./ai/AIRecommendations";

// Delivery components
import { DeliveryMapSection } from "./delivery/DeliveryMapSection";
```

### Пример использования:

```tsx
export function CheckoutForm() {
  return (
    <form>
      {/* Core UI */}
      <PersonSelector 
        numberOfPeople={numberOfPeople}
        onChange={setNumberOfPeople}
      />

      {/* AI Features */}
      {aiRecommendation && (
        <AIRecommendationCard recommendation={aiRecommendation} />
      )}

      {/* Delivery */}
      <DeliveryMapSection 
        mapLocation={mapLocation}
        deliveryInfo={deliveryInfo}
      />
    </form>
  );
}
```

## 🚀 Преимущества новой структуры

### ✅ **Масштабируемость**
- Легко добавлять новые AI компоненты в `ai/`
- Новые варианты доставки → `delivery/`
- Core компоненты растут независимо

### ✅ **Читаемость**
- Понятно, где искать нужный компонент
- Логическая группировка по функциональности
- Меньше когнитивной нагрузки

### ✅ **Тестирование**
- `core/` - unit тесты без моков API
- `ai/` - моки AI endpoint'ов
- `delivery/` - моки карт и геолокации

### ✅ **Переиспользование**
- Core компоненты можно использовать везде
- AI компоненты изолированы от бизнес-логики
- Delivery компоненты независимы

## 🔮 Будущее расширение

### Планируемые компоненты:

**Core:**
- `FormFields.tsx` - переиспользуемые поля формы
- `CustomerBadge.tsx` - бейдж постоянного клиента
- `SubmitButton.tsx` - кнопка отправки с состояниями

**AI:**
- `AIChat.tsx` - полноценный чат-ассистент
- `AIVoiceInput.tsx` - голосовой ввод заказа
- `AIPromoSuggestions.tsx` - персональные промо-акции

**Delivery:**
- `LiveTracking.tsx` - отслеживание курьера в реальном времени
- `DeliveryTimeSlot.tsx` - выбор временного слота
- `AlternativeAddresses.tsx` - сохранённые адреса

## 📊 Текущая статистика

```
core/         ~250 строк  (2 файла)
ai/           ~150 строк  (2 файла)
delivery/     ~130 строк  (1 файл)
checkout-form ~960 строк  (1 файл, оркестратор)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Итого:        ~1490 строк (6 файлов)
```

**Было:** 1343 строки в 1 файле  
**Стало:** 1490 строк в 6 файлах (с улучшенной структурой)

## 🎨 Соглашения о кодировании

1. **Именование файлов:** PascalCase для компонентов (`PersonSelector.tsx`)
2. **Именование папок:** lowercase (`core/`, `ai/`, `delivery/`)
3. **Экспорты:** Named exports для переиспользования
4. **Props:** Явные TypeScript интерфейсы
5. **Комментарии:** Эмодзи для быстрой навигации (🤖 AI, 🚚 Delivery, etc.)

---

**Последнее обновление:** 4 февраля 2026  
**Версия структуры:** 2.0 (Modular)
