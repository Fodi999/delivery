# 🏗️ AI System Architecture v2.0

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────┐    ┌──────────────────┐    ┌───────────────┐ │
│  │  checkout-form   │    │  cart store      │    │  app context  │ │
│  │  .tsx            │───▶│  cart-store.ts   │    │  app-context  │ │
│  └──────────────────┘    └──────────────────┘    └───────────────┘ │
│           │                                                          │
│           │ Phone input detected                                    │
│           ▼                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐│
│  │           useCustomerLookup Hook                                 ││
│  │           lib/hooks/use-customer-lookup.ts                       ││
│  └─────────────────────────────────────────────────────────────────┘│
│           │                                                          │
└───────────┼──────────────────────────────────────────────────────────┘
            │
            │ GET /api/customers?phone=...
            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      BACKEND API ROUTES                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  GET /api/customers                                           │  │
│  │  app/api/customers/route.ts                                   │  │
│  │  ────────────────────────────────────────────────────────────│  │
│  │  • Поиск в БД по телефону                                     │  │
│  │  • Подсчет статистики (заказы, сумма)                        │  │
│  │  • Возврат CustomerData                                       │  │
│  └──────────────────────────────────────────────────────────────┘  │
│           │                                                          │
│           │ If returning customer found                             │
│           ▼                                                          │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  POST /api/ai/welcome                                         │  │
│  │  app/api/ai/welcome/route.ts                                  │  │
│  │  ────────────────────────────────────────────────────────────│  │
│  │  Input: CustomerStats + language                             │  │
│  │  Output: { welcomeMessage, description, meta }               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│           │                                                          │
│           │ Calls AI functions                                      │
│           ▼                                                          │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │             lib/groq.ts (AI Core)                             │  │
│  │  ────────────────────────────────────────────────────────────│  │
│  │                                                                │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  generateWelcomeMessage()                              │  │  │
│  │  │  • Temperature: 0.35 (stable)                          │  │  │
│  │  │  • Context-aware prompt                                │  │  │
│  │  │  • Returns: AIResponse { text, confidence, source }    │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                           │                                   │  │
│  │                           │ Groq API call                     │  │
│  │                           ▼                                   │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │          Groq AI (LLaMA 3.3 70B)                       │  │  │
│  │  │          External Service                              │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                           │                                   │  │
│  │                           │ AI response                       │  │
│  │                           ▼                                   │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  generateOrderDescription()                            │  │  │
│  │  │  • Temperature: 0.5 (balanced)                         │  │  │
│  │  │  • Context-aware prompt                                │  │  │
│  │  │  • Returns: AIResponse { text, confidence, source }    │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                                                                │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  POST /api/ai/upsell              [NEW!]                     │  │
│  │  app/api/ai/upsell/route.ts                                  │  │
│  │  ────────────────────────────────────────────────────────────│  │
│  │  Input: { cart, favoriteCategory, timeOfDay, language }     │  │
│  │  Output: { message, reason, itemId, meta }                  │  │
│  └──────────────────────────────────────────────────────────────┘  │
│           │                                                          │
│           │ Calls AI function                                       │
│           ▼                                                          │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  generateUpsellHint()              [NEW!]                    │  │
│  │  • Temperature: 0.4 (precise)                                │  │
│  │  • Analyzes cart context                                     │  │
│  │  • Silent upsell (не продаёт, советует)                     │  │
│  │  • Returns: UpsellHintResponse                               │  │
│  │    { text, reason, itemId, confidence, source }              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
            │
            │ AI responses with metadata
            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     DATA LAYER (Prisma)                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────────────┐  │
│  │   Customer   │    │    Order     │    │   OrderItem          │  │
│  │   ────────   │    │   ─────────  │    │   ──────────         │  │
│  │   phone      │◀───│   customerId │    │   orderId            │  │
│  │   name       │    │   total      │◀───│   name               │  │
│  │   address    │    │   status     │    │   quantity           │  │
│  │   orders[]   │    │   items[]    │    │   price              │  │
│  └──────────────┘    └──────────────┘    └──────────────────────┘  │
│                                                                      │
│  Statistics Calculated:                                             │
│  • totalOrders = COUNT(orders)                                      │
│  • completedOrders = COUNT(orders WHERE status = DONE)              │
│  • totalSpent = SUM(total WHERE status = DONE)                      │
│  • isVIP = totalOrders >= 10 OR totalSpent >= 50000                │
│  • favoriteCategory = MOST_FREQUENT(items.category)                 │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow

### Flow 1: Customer Recognition + AI Welcome
```
1. User enters phone → useCustomerLookup()
2. Hook calls GET /api/customers?phone=...
3. API queries database (Prisma)
4. Calculate statistics (orders, spent, VIP status)
5. If returning customer:
   a. Call POST /api/ai/welcome
   b. Generate AI greeting (temperature 0.35)
   c. Generate AI compliment (temperature 0.5)
   d. Return structured response with metadata
6. Frontend shows personalized badge
```

### Flow 2: Smart Upsell (NEW!)
```
1. User adds 2+ items to cart
2. Frontend calls POST /api/ai/upsell
3. AI analyzes:
   - Cart categories
   - Missing items (no drinks?)
   - Favorite category
   - Time of day
4. Generate recommendation (temperature 0.4)
5. Return: { message, reason, confidence }
6. If confidence === "high" → show hint
```

---

## 📊 AI Response Structure

```typescript
interface AIResponse {
  type: "welcome" | "compliment" | "upsell"
  text: string                           // Текст для показа
  confidence: "high" | "medium" | "low"  // Уверенность AI
  source: "ai" | "fallback"              // Откуда ответ
  metadata?: {
    model: "llama-3.3-70b-versatile"
    temperature: 0.35 | 0.5 | 0.4
    tokens: number                        // Использовано токенов
  }
}
```

---

## 🎯 Temperature Strategy

```
Welcome Message:  0.35  ━━━━━━━━░░░░░░░░░░  Stable & Professional
                        ▲
                        └─ Нужна стабильность

Compliment:       0.5   ━━━━━━━━━━░░░░░░░░  Balanced Creativity
                        ▲
                        └─ Немного креатива

Upsell Hint:      0.4   ━━━━━━━━░░░░░░░░░░  Precise Suggestions
                        ▲
                        └─ Точные рекомендации
```

---

## 🚦 Confidence Levels

```
HIGH (0.8-1.0)    ━━━━━━━━━━  Always show
                  ▲
                  └─ AI уверен, показываем всегда

MEDIUM (0.5-0.7)  ━━━━━░░░░░  Show with fallback ready
                  ▲
                  └─ Показываем, но готовы к fallback

LOW (0.0-0.4)     ━━░░░░░░░░  Use fallback
                  ▲
                  └─ Используем дефолтное сообщение
```

---

## 🔑 Key Components

### Core
- `lib/groq.ts` - AI functions (400+ lines)
- `lib/hooks/use-customer-lookup.ts` - Customer recognition hook

### API Routes
- `app/api/customers/route.ts` - Database lookup
- `app/api/ai/welcome/route.ts` - Welcome AI
- `app/api/ai/upsell/route.ts` - Upsell AI [NEW]

### Frontend
- `components/checkout/checkout-form.tsx` - Main form
- `components/checkout/ai/AIRecommendations.tsx` - AI cards

---

## 📈 Metrics to Track

```
┌──────────────────────┐
│   AI Performance     │
├──────────────────────┤
│ • Response time      │
│ • Fallback rate      │
│ • Confidence dist    │
│ • Token usage        │
└──────────────────────┘
         │
         │
         ▼
┌──────────────────────┐
│   Business Impact    │
├──────────────────────┤
│ • Upsell conversion  │
│ • Cart size increase │
│ • Customer retention │
│ • Order frequency    │
└──────────────────────┘
```

---

**Version:** 2.0  
**Last Updated:** 4 февраля 2026  
**Status:** ✅ Production
